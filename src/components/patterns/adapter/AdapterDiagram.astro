<div class="diagramWrap">
  <section class="panel diagram">
    <div class="diagramHead">
      <h2>Diagrama: flujo del Adapter</h2>
      <span class="hint">Client usa <b>Target.request()</b>. Adapter traduce a <b>Adaptee.specificRequest()</b></span>
    </div>

    <div class="flowStage" id="adapterFlowStage">
      <canvas class="flowCanvas" id="adapterFlowCanvas" aria-hidden="true"></canvas>

      <div class="flowLegend" style="margin-top: 12px;">
        <span class="miniPill" id="adapterModePill">Adapter: ON</span>
        <span class="miniPill" id="adapterCallPill">idle</span>
      </div>
    </div>
  </section>
</div>

<style>
  .diagramWrap { margin: 14px 0 12px; }
  .diagram { position: relative; overflow: hidden; }
  .diagramHead { display: flex; align-items: center; justify-content: space-between; gap: 10px; margin-bottom: 10px; }
  .diagramHead h2 { margin: 0; font-size: 16px; }
  .hint { color: var(--muted); font-size: 13px; }

  .flowStage {
    position: relative;
    background: rgba(255,255,255,.03);
    border: 1px solid rgba(255,255,255,.08);
    border-radius: 14px;
    padding: 14px;
    min-height: 148px;
  }

  .flowCanvas {
    display: block;
    width: 100%;
    height: 210px;
    border-radius: 12px;
    background: rgba(0,0,0,.10);
    border: 1px solid rgba(255,255,255,.08);
  }

  .flowLegend { margin-top: 10px; display: flex; gap: 10px; flex-wrap: wrap; color: var(--muted); font-size: 12px; }

  .miniPill {
    font-size: 12px;
    padding: 4px 8px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,.12);
    color: var(--muted);
    background: rgba(0,0,0,.12);
  }
  .miniPill.on { color: var(--accent); background: rgba(93,214,255,.10); border-color: rgba(93,214,255,.35); }

  @media (max-width: 520px) {
    .diagramHead { flex-direction: column; align-items: flex-start; }
    .diagramHead h2 { font-size: 15px; }
    .hint { font-size: 12.5px; line-height: 1.25; }

    .flowStage { padding: 12px; min-height: 0; }
    .flowCanvas { height: 190px; border-radius: 10px; }
    .flowLegend { gap: 8px; margin-top: 10px; }
    .miniPill { font-size: 11.5px; padding: 3px 7px; }
  }
</style>
