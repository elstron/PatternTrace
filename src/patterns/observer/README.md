# Observer

Estructura para agregar el patrón Observer como una demo aislada.

- `meta.ts`: metadata (slug/títulos)
- `ObserverPage.astro`: página que compone la demo
- `observer-logic.js`: lógica del demo (DOM + canvas + tabs)
- `components/`: piezas visuales de la demo
