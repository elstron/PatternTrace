<div class="diagramWrap">
  <section class="panel diagram">
    <div class="diagramHead">
      <h2>Diagrama: flujo de notificación</h2>
      <span class="hint"
        >Al hacer <b>notify()</b>, las flechas “viajan” a cada observer suscrito</span
      >
    </div>

    <div class="flowStage" id="flowStage">
      <canvas class="flowCanvas" id="flowCanvas" aria-hidden="true"></canvas>

      <div class="flowLegend" style="margin-top: 12px;">
        <span><span class="dot sub"></span> Subject</span>
        <span><span class="dot obs"></span> Observer</span>
        <span class="miniPill" id="subjectPill">state</span>
        <span class="miniPill" id="notifyPill">idle</span>
        <span class="miniPill on" id="obsCountPill">—</span>
      </div>
    </div>
  </section>
</div>

<style>
  .diagramWrap {
    margin: 14px 0 12px;
  }
  .diagram {
    position: relative;
    overflow: hidden;
  }
  .diagramHead {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 10px;
  }
  .diagramHead h2 {
    margin: 0;
    font-size: 16px;
  }
  .hint {
    color: var(--muted);
    font-size: 13px;
  }

  .flowStage {
    position: relative;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 14px;
    padding: 14px;
    min-height: 148px;
  }

  .flowCanvas {
    display: block;
    width: 100%;
    height: 220px;
    border-radius: 12px;
    background: rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.08);
  }

  .flowLegend {
    margin-top: 10px;
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    color: var(--muted);
    font-size: 12px;
  }
  .dot {
    width: 10px;
    height: 10px;
    border-radius: 999px;
    display: inline-block;
    margin-right: 6px;
  }
  .dot.sub {
    background: rgba(92, 255, 138, 0.95);
  }
  .dot.obs {
    background: rgba(93, 214, 255, 0.95);
  }

  .miniPill {
    font-size: 12px;
    padding: 4px 8px;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    color: var(--muted);
    background: rgba(0, 0, 0, 0.12);
  }
  .miniPill.on {
    color: var(--accent);
    background: rgba(93, 214, 255, 0.1);
    border-color: rgba(93, 214, 255, 0.35);
  }

  @media (max-width: 520px) {
    .diagramHead {
      flex-direction: column;
      align-items: flex-start;
    }
    .diagramHead h2 {
      font-size: 15px;
    }
    .hint {
      font-size: 12.5px;
      line-height: 1.25;
    }

    .flowStage {
      padding: 12px;
      min-height: 0;
    }
    .flowCanvas {
      height: 170px;
      border-radius: 10px;
    }
    .flowLegend {
      gap: 8px;
      margin-top: 10px;
    }
    .miniPill {
      font-size: 11.5px;
      padding: 3px 7px;
    }
    .dot {
      width: 9px;
      height: 9px;
      margin-right: 5px;
    }
  }
</style>
