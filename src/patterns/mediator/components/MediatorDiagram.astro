<section class="panel">
  <div class="title">
    <h2 style="margin:0; font-size:16px;">Diagrama</h2>
    <span class="badge">canvas</span>
  </div>

  <div id="mediatorFlowStage" class="stage">
    <canvas id="mediatorFlowCanvas"></canvas>
    <div class="pills">
      <span id="mediatorModePill" class="pill on">mode: mediated</span>
      <span id="mediatorEventPill" class="pill">idle</span>
    </div>
  </div>
</section>

<style>
  .badge {
    font-size: 12px;
    padding: 5px 9px;
    border-radius: 999px;
    background: rgba(93, 214, 255, .10);
    border: 1px solid rgba(93, 214, 255, .20);
    color: rgba(93, 214, 255, .95);
  }

  .stage {
    margin-top: 10px;
    height: 230px;
    border-radius: 14px;
    background: rgba(0,0,0,.18);
    border: 1px dashed rgba(255,255,255,.14);
    position: relative;
    overflow: hidden;
  }

  canvas {
    width: 100%;
    height: 100%;
    display: block;
  }

  .pills {
    position: absolute;
    left: 10px;
    bottom: 10px;
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }

  .pill {
    font-size: 12px;
    padding: 4px 8px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,.12);
    color: var(--muted);
    background: rgba(0,0,0,.12);
  }

  .pill.on {
    border-color: rgba(93, 214, 255, .30);
    background: rgba(93, 214, 255, .10);
    color: rgba(93, 214, 255, .95);
  }
</style>
