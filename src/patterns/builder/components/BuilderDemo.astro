<section class="panel">
  <div class="title">
    <h2 style="margin:0; font-size:16px;">Demo (Builder)</h2>
    <span class="badge">build()</span>
  </div>

  <p class="hint" style="margin-top: 0;">
    Elige una variante y ejecuta el proceso de construcción. Observa el flujo en el diagrama.
  </p>

  <div class="row" style="margin-top: 10px;">
    <button class="primary" id="btnRunBuilder">Director.build()</button>
    <button id="btnToggleBuilderVariant">Variant: Minimal</button>
  </div>

  <div class="valueLine" style="margin-top: 10px;">
    <strong>Resultado</strong>
    <span id="builderResult" class="valuePill">—</span>
  </div>

  <div id="builderLog" class="log"></div>
</section>

<style>
  .badge {
    font-size: 12px;
    padding: 5px 9px;
    border-radius: 999px;
    background: rgba(93, 255, 160, .10);
    border: 1px solid rgba(93, 255, 160, .20);
    color: rgba(112, 255, 178, .95);
  }

  .hint { color: var(--muted); font-size: 13px; }

  .valueLine {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 12px;
    background: rgba(255,255,255,.04);
    border: 1px solid rgba(255,255,255,.10);
    border-radius: 12px;
  }

  .valueLine strong {
    font-size: 13px;
    color: var(--muted);
    font-weight: 600;
  }

  .valuePill {
    padding: 6px 10px;
    border-radius: 999px;
    background: rgba(93, 255, 160, .10);
    border: 1px solid rgba(93, 255, 160, .20);
    color: rgba(112, 255, 178, .95);
    font-weight: 800;
    font-size: 13px;
  }

  .log {
    margin-top: 12px;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
    font-size: 12px;
    background: rgba(0,0,0,.22);
    border: 1px solid rgba(255,255,255,.08);
    border-radius: 12px;
    padding: 10px;
    color: rgba(230,238,252,.88);
    max-height: 220px;
    overflow: auto;
  }

  .log :global(.muted) { color: rgba(159,178,214,.85); }

  :global(.flash) { animation: flash .6s ease; }
  @keyframes flash {
    0% { box-shadow: 0 0 0 rgba(93, 255, 160, 0); }
    30% { box-shadow: 0 0 0 6px rgba(93, 255, 160, .16); }
    100% { box-shadow: 0 0 0 rgba(93, 255, 160, 0); }
  }
</style>
